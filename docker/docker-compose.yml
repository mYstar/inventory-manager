services:
  inventory_manager:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      - DB_FILE=${DB_FILE}
    ports:
      - "${API_PORT}:80"
    volumes:
      - ./data/:/app/data/